/**
 * 路由配置 要使用自动路由 router = [] 自动路由只支持一级路由
 * 
 * @example [{
 *        entryName: "index",
 *        app: "./src/app/index.ts",
 *        filename: `index`,//生成文件名称
 *    }]
 * 
 */
const router = []

module.exports = router