/**
 * 路由配置 要使用自动路由 router = []
 */
const router = [
    {
        entryName: "index",
        controller: "./src/controller/index.ts",
        filename: `index`,//生成文件名称
    },
]

module.exports = router